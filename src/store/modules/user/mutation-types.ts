/* ============
 * Mutation types for the counter module
 * ============
 *
 * The mutation types that are available
 * on the counter module.
 */
import keyMirror from 'keymirror';

export default keyMirror({
  USER_LIST: null,
});
